# Layerset: Layerset and Layer Build

Kaptain layerset composing the full build configuration for layer and layerset projects.

Composes these layers in order:

1. **layer-github-flow-strict** — strict GitHub flow quality enforcement
2. **layer-and-layerset-build** — multi-arch docker builds, 2-part versioning
